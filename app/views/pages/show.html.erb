<div class='container'>
<p style="color: green"><%= notice %></p>
<div>
    <strong><%=t('page_number',page_number: @page.page_number)%></strong>
  </p>

  <p>
    <strong><%=t('text')%></strong>
    <%= @page.text %>
  </p>
</div>
<div >
  <%= link_to t('edit'), edit_user_writing_page_url(@user, @writing, @page) %> |
    <%=link_to t('back'), new_user_writing_page_url(@user, @writing)%>

  <%= button_to t('destroy'), user_writing_page_path(@writing, @page), method: :delete %>
</div>
</div>